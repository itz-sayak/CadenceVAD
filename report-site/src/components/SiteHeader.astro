<header class="terminal-header">
  <div class="prompt-line" aria-label="Project location">
    <span class="prompt-path">~ cadencevad</span>
    <span class="prompt-branch">main</span>
    <span class="prompt-arrow" aria-hidden="true">❯</span>
  </div>

  <div class="nav-line">
    <a class="brand" href="#top" aria-label="CadenceVAD report home">
      <span class="brand-org">itz-sayak/</span><span>cadencevad</span><span class="cursor" aria-hidden="true"></span>
    </a>
    <nav aria-label="Report sections">
      <a href="#try-it">test</a>
      <a href="#model-preview">compare</a>
      <a href="#benchmarks">speed</a>
      <a href="#colab">colab</a>
      <a href="#integration">integrate</a>
      <a href="#accuracy">accuracy</a>
      <a class="nav-cta" href="#verdict">verdict</a>
    </nav>
  </div>
</header>

<style>
  /* Sticky glass bar. Kept in normal flow so the page rhythm below is
     unchanged; only the surface and offset differ from the flat rule it
     replaces. */
  .terminal-header {
    position: sticky;
    top: 12px;
    z-index: 20;
    margin-top: 12px;
    padding: 16px 22px 14px;
    border: 1px solid var(--glass-border-lit);
    border-radius: var(--radius);
    background: rgba(14, 16, 12, 0.72);
    box-shadow:
      0 1px 0 0 rgba(255, 255, 255, 0.08) inset,
      0 20px 44px -30px rgba(0, 0, 0, 1);
    font-family: var(--mono);
    backdrop-filter: blur(26px) saturate(170%);
    -webkit-backdrop-filter: blur(26px) saturate(170%);
  }

  .prompt-line,
  .nav-line {
    display: flex;
    align-items: center;
  }

  .prompt-line {
    gap: 8px;
    margin-bottom: 15px;
    color: var(--muted);
    font-size: 0.74rem;
    letter-spacing: 0.04em;
  }

  .prompt-path {
    color: var(--signal);
  }

  .prompt-branch {
    color: var(--cyan);
  }

  .prompt-arrow {
    color: var(--text);
  }

  .nav-line {
    justify-content: space-between;
    gap: 28px;
  }

  .brand {
    display: inline-flex;
    align-items: center;
    min-width: max-content;
    color: var(--signal);
    font-size: clamp(1.05rem, 2vw, 1.35rem);
    font-weight: 650;
    letter-spacing: -0.04em;
  }

  .brand-org {
    color: var(--text-soft);
    font-weight: 450;
  }

  .cursor {
    width: 0.52em;
    height: 1.05em;
    margin-left: 4px;
    background: var(--signal);
    animation: blink 1.1s steps(1) infinite;
  }

  nav {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    justify-content: flex-end;
    gap: 4px;
  }

  nav a {
    padding: 8px 9px;
    color: var(--muted);
    font-size: 0.74rem;
    letter-spacing: 0.02em;
    transition: color 140ms ease, background 140ms ease;
  }

  nav a::before {
    content: "./";
    color: var(--dim);
  }

  nav a:hover,
  nav a:focus-visible {
    border-radius: var(--radius-pill);
    color: var(--text);
    background: rgba(232, 232, 226, 0.08);
  }

  nav .nav-cta {
    background: transparent;
    color: var(--signal);
  }

  @keyframes blink {
    50% {
      opacity: 0;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .cursor {
      animation: none;
    }
  }

  @media (max-width: 760px) {
    .terminal-header {
      padding: 14px 16px 12px;
    }

    .nav-line {
      align-items: flex-start;
      flex-direction: column;
      gap: 14px;
    }

    nav {
      justify-content: flex-start;
      width: 100%;
    }

    nav a {
      padding-left: 0;
      padding-right: 14px;
    }
  }
</style>
